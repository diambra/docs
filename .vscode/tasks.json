{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Hugo Server",
            "type": "shell",
            "command": "hugo",
            "args": ["server", "-w", "--ignoreCache", "--verbose"],
            "group": {
                "kind": "build"
            }
        }
    ]
}
