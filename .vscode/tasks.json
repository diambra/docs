{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Hugo Server",
            "type": "shell",
            "command": "hugo",
            "args": ["server", "-w"],
            "group": {
                "kind": "build"
            }
        }
    ]
}
